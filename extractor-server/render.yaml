services:
  - type: web
    name: asmusic-extractor
    runtime: python
    rootDir: extractor-server
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --timeout 300 --workers 1 --bind 0.0.0.0:$PORT
    plan: free
